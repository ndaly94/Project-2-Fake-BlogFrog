config:
  target: 'http://localhost:3000'
  phases:
    - duration: 1
      arrivalRate: 3
scenarios:
  - flow:
      - post:
          url: "/users"
          json:
            username: "{{ $randomString() }}"
            email: "{{ $randomString() }}"
            password: "Password123"
